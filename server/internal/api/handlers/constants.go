package handlers

// Shared error strings used across handlers when writing an APIResponse.
const (
	InternalServerError     = "INTERNAL_SERVER_ERROR"
	InternalServerErrorDesc = "an unexpected error occurred, please try again later"
	EnvironmentNotFound     = "environment not found"
)
